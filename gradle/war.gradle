apply plugin: "war"

bootWar {
    mainClass = "com.clangenginner.book.KhBookApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
